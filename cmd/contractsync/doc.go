// Command contractsync regenerates the vendored Aeries contract files from the
// checked-in source snapshots.
//
// This tool intentionally works offline so the repository keeps a deterministic
// view of the documentation snapshot that the SDK was built and tested against.
package main
